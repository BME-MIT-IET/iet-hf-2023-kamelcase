package softprojlab.cli;

/**
 * Returns a string representation of the object in YAML format.
 */
public interface YAMLExportable {
    /**
     * Returns a string representation of the object in YAML format.
     * @return String representation of the object in YAML format.
     */
    public String toYAML();
}
